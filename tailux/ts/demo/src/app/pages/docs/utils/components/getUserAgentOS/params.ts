import { type ParamItem } from "@/components/docs/demo/DemoLayout/Params";

export const params: ParamItem[] = [
  {
    name: "userAgent",
    type: "string",
    description:
      "Optional. The user agent string to analyze. Defaults to navigator.userAgent.",
  },
];
