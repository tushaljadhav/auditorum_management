import { type ParamItem } from "@/components/docs/demo/DemoLayout/Params";

export const params: ParamItem[] = [
  {
    name: "errorMessage",
    type: "string",
    description:
      "Error message to display when the context is used outside of its provider",
  },
];
